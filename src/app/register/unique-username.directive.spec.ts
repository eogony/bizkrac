import { UniqueUsernameDirective } from './unique-username.directive';

describe('UniqueUsernameDirective', () => {
  it('should create an instance', () => {
    const directive = UniqueUsernameDirective;
    expect(directive).toBeTruthy();
  });
});
